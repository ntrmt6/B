# DueBook environment variables

# Base URL of the backend API (the Express server), WITHOUT a trailing slash
# and WITHOUT the /api suffix — the app adds /api itself.
# Example (local):  http://localhost:5001
# Example (hosted): https://your-backend-host.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:5001
